services:
  ollama:
    container_name: ollama
    image: ollama/ollama:latest
    restart: unless-stopped

    env_file:
      - ../../runtime/ollama/.env

    ports:
      - 11434:11434

    volumes:
      - ../../../../.ollama/:/root/.ollama:rw

    networks:
      - private-net

    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G
        reservations:
          cpus: "2"
          memory: 4G

networks:
  private-net:
    external: true
